# Copy to web/.env.local only when TrailBase is not on localhost:4000.
VITE_TRAILBASE_URL=http://localhost:4000

# Used by dev.sh and mcp.sh.
TRAILBASE_URL=http://localhost:4000
TRAILBASE_MCP_USER=admin@localhost
